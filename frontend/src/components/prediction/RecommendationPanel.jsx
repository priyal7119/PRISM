// src/components/prediction/RecommendationPanel.jsx

import { Lightbulb, ChevronRight } from "lucide-react";
import "../../styles/prediction.css";

function RecommendationPanel({ recommendations = [] }) {
    return (
        <div className="prediction-card recommendation-panel">

            <div className="prediction-card-header">
                <div>
                    <h2>AI Recommendations</h2>
                    <p>
                        Preventive actions generated by the AI prediction engine
                    </p>
                </div>
            </div>

            {recommendations.length === 0 ? (
                <div className="prediction-empty">
                    No recommendations available.
                </div>
            ) : (
                <div className="recommendation-list">

                    {recommendations.map((item, index) => (
                        <div
                            key={index}
                            className="recommendation-item"
                        >

                            <div className="recommendation-number">
                                <Lightbulb size={18} />
                            </div>

                            <div className="recommendation-content">

                                <h4>
                                    Recommendation {index + 1}
                                </h4>

                                <p>{item}</p>

                            </div>

                            <ChevronRight
                                size={18}
                                className="recommendation-arrow"
                            />

                        </div>
                    ))}

                </div>
            )}

        </div>
    );
}

export default RecommendationPanel;